<root main_tree_to_execute="MainTree">

  <TreeNodesModel>
    <!-- TODO: Register CalculateGoal action model here -->
    <Action ID="PrintTarget">
      <input_port name="target"/>
    </Action>
  </TreeNodesModel>

  <BehaviorTree ID="MainTree">
    <Sequence name="root">
      <!-- TODO: Swap SetBlackboard with CalculateGoal action and print its result -->
      <SetBlackboard output_key="OtherGoal" value="-1;3;0" />
      <PrintTarget target="{OtherGoal}" />
    </Sequence>
  </BehaviorTree>

</root>
